{% doc %}
  @prompt
    Create a full-width hero section with a 2-column split layout:
    
    Layout
    
    Desktop: Left 50% text, Right 50% image
    
    Mobile: stack vertically (text first, image second)
    
    White background, lots of whitespace, clean luxury look
    
    Center the left content vertically within its column
    
    LEFT COLUMN (Text – must match exactly)
    
    Large serif headline (centered):
    CLOSING THIS <span id="ny-weekday">Wednesday</span>!
    
    Under it (centered) show the date in MM/DD/YYYY:
    <span id="ny-date-top">01/28/2026</span>
    
    Under it, small uppercase label (centered, letter spaced):
    EVERYTHING IS FREE FINAL CHANCE
    
    Body paragraph block (centered, same line breaks feel, max width ~420px). Text must be exactly this (only the weekday + date must be dynamic):
    
    After an amazing journey, it’s time to close this chapter. Instead of letting our remaining inventory go to waste, I want to give it back to the people who kept this small business alive. Everything is now free until <span id="ny-weekday-para">Wednesday</span>, <span id="ny-date-para">01/28/2026</span>, at midnight.
    I’ll be fulfilling orders until then as a small thank-you for all the years of support.
    
    RIGHT COLUMN (Image)
    
    Add an image picker
    
    Image fills the right side, aligned like the reference
    
    No overlay text
    
    Object-fit cover / contain to keep it clean and premium
    
    ✅ Dynamic requirements (MUST)
    
    The weekday (“Wednesday”) must be dynamic and update daily.
    
    The date must be dynamic and update daily.
    
    Weekday + date must reflect America/New_York timezone (US New York), not the store timezone.
    
    Must update on every page load.
    
    Use JavaScript with Intl.DateTimeFormat and timeZone: "America/New_York" to populate:
    
    #ny-weekday
    
    #ny-date-top
    
    #ny-weekday-para
    
    #ny-date-para
    
    Formatting rules
    
    Weekday: full weekday name (Monday, Tuesday, etc.)
    
    Date: MM/DD/YYYY (e.g., 01/28/2026)
    
    Keep all other text exactly the same.
    
    ✅ Styling requirements (match screenshot)
    
    Headline serif, large, elegant
    
    Date slightly smaller than headline
    
    Label small uppercase with letter spacing
    
    Paragraph smaller font, centered, comfortable line height
    
    Max width container around ~1200px
    
    Section padding top/bottom ~60px
    
    Large gap between columns
    
    Name the section: “Closing Split Hero (Dynamic NY Date)”
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-closing-hero-{{ ai_gen_id }} {
    display: block;
    width: 100%;
    background-color: {{ block.settings.background_color }};
    padding: {{ block.settings.section_padding }}px 0;
  }

  .ai-closing-hero-container-{{ ai_gen_id }} {
    max-width: {{ block.settings.max_width }}px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .ai-closing-hero-grid-{{ ai_gen_id }} {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: {{ block.settings.column_gap }}px;
    align-items: center;
  }

  .ai-closing-hero-text-column-{{ ai_gen_id }} {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 40px 20px;
  }

  .ai-closing-hero-headline-{{ ai_gen_id }} {
    font-family: {{ block.settings.headline_font.family }}, {{ block.settings.headline_font.fallback_families }};
    font-weight: {{ block.settings.headline_font.weight }};
    font-style: {{ block.settings.headline_font.style }};
    font-size: {{ block.settings.headline_size }}px;
    line-height: 1.2;
    color: {{ block.settings.text_color }};
    margin: 0 0 {{ block.settings.headline_spacing }}px 0;
  }

  .ai-closing-hero-date-top-{{ ai_gen_id }} {
    font-family: {{ block.settings.headline_font.family }}, {{ block.settings.headline_font.fallback_families }};
    font-weight: {{ block.settings.headline_font.weight }};
    font-style: {{ block.settings.headline_font.style }};
    font-size: {{ block.settings.date_size }}px;
    line-height: 1.3;
    color: {{ block.settings.text_color }};
    margin: 0 0 {{ block.settings.date_spacing }}px 0;
  }

  .ai-closing-hero-label-{{ ai_gen_id }} {
    font-size: {{ block.settings.label_size }}px;
    text-transform: uppercase;
    letter-spacing: {{ block.settings.label_letter_spacing }}px;
    color: {{ block.settings.text_color }};
    margin: 0 0 {{ block.settings.label_spacing }}px 0;
  }

  .ai-closing-hero-paragraph-{{ ai_gen_id }} {
    font-size: {{ block.settings.paragraph_size }}px;
    line-height: {{ block.settings.paragraph_line_height }};
    color: {{ block.settings.text_color }};
    max-width: {{ block.settings.paragraph_max_width }}px;
    margin: 0;
  }

  .ai-closing-hero-image-column-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    height: 100%;
    min-height: 500px;
  }

  .ai-closing-hero-image-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    object-fit: {{ block.settings.image_fit }};
    display: block;
  }

  .ai-closing-hero-image-placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    background-color: #f4f4f4;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 500px;
  }

  .ai-closing-hero-image-placeholder-{{ ai_gen_id }} svg {
    width: 100%;
    height: 100%;
    max-width: 400px;
    max-height: 400px;
  }

  @media screen and (max-width: 749px) {
    .ai-closing-hero-grid-{{ ai_gen_id }} {
      grid-template-columns: 1fr;
      gap: 0;
    }

    .ai-closing-hero-text-column-{{ ai_gen_id }} {
      order: 1;
      padding: 30px 15px;
    }

    .ai-closing-hero-image-column-{{ ai_gen_id }} {
      order: 2;
      min-height: 400px;
    }

    .ai-closing-hero-headline-{{ ai_gen_id }} {
      font-size: calc({{ block.settings.headline_size }}px * 0.7);
    }

    .ai-closing-hero-date-top-{{ ai_gen_id }} {
      font-size: calc({{ block.settings.date_size }}px * 0.7);
    }

    .ai-closing-hero-label-{{ ai_gen_id }} {
      font-size: calc({{ block.settings.label_size }}px * 0.85);
    }

    .ai-closing-hero-paragraph-{{ ai_gen_id }} {
      font-size: calc({{ block.settings.paragraph_size }}px * 0.9);
    }
  }
{% endstyle %}

<closing-hero-{{ ai_gen_id }} class="ai-closing-hero-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  <div class="ai-closing-hero-container-{{ ai_gen_id }}">
    <div class="ai-closing-hero-grid-{{ ai_gen_id }}">
      <div class="ai-closing-hero-text-column-{{ ai_gen_id }}">
        <h1 class="ai-closing-hero-headline-{{ ai_gen_id }}">
          CLOSING THIS <span id="ny-weekday-{{ ai_gen_id }}">Wednesday</span>!
        </h1>
        
        <div class="ai-closing-hero-date-top-{{ ai_gen_id }}">
          <span id="ny-date-top-{{ ai_gen_id }}">01/28/2026</span>
        </div>
        
        <div class="ai-closing-hero-label-{{ ai_gen_id }}">
          EVERYTHING IS FREE FINAL CHANCE
        </div>
        
        <p class="ai-closing-hero-paragraph-{{ ai_gen_id }}">
          After an amazing journey, it's time to close this chapter. Instead of letting our remaining inventory go to waste, I want to give it back to the people who kept this small business alive. Everything is now free until <span id="ny-weekday-para-{{ ai_gen_id }}">Wednesday</span>, <span id="ny-date-para-{{ ai_gen_id }}">01/28/2026</span>, at midnight.<br>I'll be fulfilling orders until then as a small thank-you for all the years of support.
        </p>
      </div>
      
      <div class="ai-closing-hero-image-column-{{ ai_gen_id }}">
        {% if block.settings.image %}
          <img
            src="{{ block.settings.image | image_url: width: 1200 }}"
            alt="{{ block.settings.image.alt | escape }}"
            loading="lazy"
            width="{{ block.settings.image.width }}"
            height="{{ block.settings.image.height }}"
            class="ai-closing-hero-image-{{ ai_gen_id }}"
          >
        {% else %}
          <div class="ai-closing-hero-image-placeholder-{{ ai_gen_id }}">
            {{ 'image' | placeholder_svg_tag }}
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</closing-hero-{{ ai_gen_id }}>

<script>
  (function() {
    class ClosingHero{{ ai_gen_id }} extends HTMLElement {
      constructor() {
        super();
      }

      connectedCallback() {
        this.updateDynamicDates();
      }

      updateDynamicDates() {
        const nyTimeZone = 'America/New_York';
        
        const now = new Date();
        
        const weekdayFormatter = new Intl.DateTimeFormat('en-US', {
          weekday: 'long',
          timeZone: nyTimeZone
        });
        
        const dateFormatter = new Intl.DateTimeFormat('en-US', {
          month: '2-digit',
          day: '2-digit',
          year: 'numeric',
          timeZone: nyTimeZone
        });
        
        const weekday = weekdayFormatter.format(now);
        const dateString = dateFormatter.format(now);
        
        const weekdayElement = this.querySelector('#ny-weekday-{{ ai_gen_id }}');
        const dateTopElement = this.querySelector('#ny-date-top-{{ ai_gen_id }}');
        const weekdayParaElement = this.querySelector('#ny-weekday-para-{{ ai_gen_id }}');
        const dateParaElement = this.querySelector('#ny-date-para-{{ ai_gen_id }}');
        
        if (weekdayElement) weekdayElement.textContent = weekday;
        if (dateTopElement) dateTopElement.textContent = dateString;
        if (weekdayParaElement) weekdayParaElement.textContent = weekday;
        if (dateParaElement) dateParaElement.textContent = dateString;
      }
    }

    customElements.define('closing-hero-{{ ai_gen_id }}', ClosingHero{{ ai_gen_id }});
  })();
</script>

{% schema %}
{
  "name": "Closing split hero",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "max_width",
      "min": 1000,
      "max": 1600,
      "step": 50,
      "unit": "px",
      "label": "Maximum width",
      "default": 1200
    },
    {
      "type": "range",
      "id": "section_padding",
      "min": 20,
      "max": 120,
      "step": 10,
      "unit": "px",
      "label": "Section padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "column_gap",
      "min": 20,
      "max": 100,
      "step": 10,
      "unit": "px",
      "label": "Column gap",
      "default": 60
    },
    {
      "type": "header",
      "content": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "select",
      "id": "image_fit",
      "label": "Image fit",
      "options": [
        {
          "value": "cover",
          "label": "Cover"
        },
        {
          "value": "contain",
          "label": "Contain"
        }
      ],
      "default": "cover"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Headline"
    },
    {
      "type": "font_picker",
      "id": "headline_font",
      "label": "Font",
      "default": "serif"
    },
    {
      "type": "range",
      "id": "headline_size",
      "min": 30,
      "max": 80,
      "step": 2,
      "unit": "px",
      "label": "Size",
      "default": 56
    },
    {
      "type": "range",
      "id": "headline_spacing",
      "min": 10,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Bottom spacing",
      "default": 20
    },
    {
      "type": "header",
      "content": "Date"
    },
    {
      "type": "range",
      "id": "date_size",
      "min": 20,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Size",
      "default": 42
    },
    {
      "type": "range",
      "id": "date_spacing",
      "min": 10,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Bottom spacing",
      "default": 24
    },
    {
      "type": "header",
      "content": "Label"
    },
    {
      "type": "range",
      "id": "label_size",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Size",
      "default": 12
    },
    {
      "type": "range",
      "id": "label_letter_spacing",
      "min": 0,
      "max": 5,
      "step": 0.5,
      "unit": "px",
      "label": "Letter spacing",
      "default": 2
    },
    {
      "type": "range",
      "id": "label_spacing",
      "min": 10,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Bottom spacing",
      "default": 30
    },
    {
      "type": "header",
      "content": "Paragraph"
    },
    {
      "type": "range",
      "id": "paragraph_size",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Size",
      "default": 17
    },
    {
      "type": "range",
      "id": "paragraph_line_height",
      "min": 1,
      "max": 2,
      "step": 0.1,
      "label": "Line height",
      "default": 1.6
    },
    {
      "type": "range",
      "id": "paragraph_max_width",
      "min": 300,
      "max": 600,
      "step": 20,
      "unit": "px",
      "label": "Maximum width",
      "default": 420
    }
  ],
  "presets": [
    {
      "name": "Closing split hero"
    }
  ]
}
{% endschema %}
